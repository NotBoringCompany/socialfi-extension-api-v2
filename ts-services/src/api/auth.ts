import { APIResponse } from '../utils/api';
import { ExtendedTwitterProfile } from '../utils/customProfiles';

export const handleTwitterAuth = async (twitterId: string, profile?: ExtendedTwitterProfile): Promise<APIResponse> => {

}